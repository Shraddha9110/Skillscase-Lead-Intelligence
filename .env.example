# Optional. When set, classification, enrichment, critic review, and outreach
# use an OpenAI-compatible chat API instead of the built-in analyst.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OPENAI_BASE_URL=https://api.openai.com/v1
